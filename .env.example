# Phase 0 local database (matches infra/docker-compose.yml).
# Copy to .env and adjust for Neon/Supabase/any Postgres URL if not using Docker.
DATABASE_URL=postgres://prepforge:prepforge@localhost:5432/prepforge
